name: Generate Index
on:
  schedule:
  - cron: "15 * * * *"
jobs:
  generate-index:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Go
        uses: actions/setup-go@v1
        with:
          go-version: 1.16
      - name: Checkout
        uses: actions/checkout@v2
      - name: Generate index
        run: |
          curl -OL https://github.com/ghokun/climan/releases/download/v0.0.3/climan-linux-amd64
          mv climan-linux-amd64 climan
          chmod +x climan
          echo '```' > ./docs/index.md; ./climan list >> ./docs/index.md; echo '```' >> ./docs/index.md
      - name: Commit changes
        run: |
          git config --global user.name 'ghokun'
          git config --global user.email 'ghokun@users.noreply.github.com'
          git add .
          git commit -am "Actions commit" || echo "No changes to commit"
          git push
